name: another
root: ~/Code/Clipchamp/another-cc-stack
windows:
    - vim:
        panes:
            - vim
    - git:
        layout: main-vertical
        panes:
            - #empty
            - #empty
    - backend:
        panes:
            - . .venv39/bin/activate && cd backend
